module.exports = {
    somar: (a, b) => parseFloat(a) + parseFloat(b),
    subtrair: (a, b) => parseFloat(a) - parseFloat(b),
    multplicar: (a, b) => parseFloat(a) * parseFloat(b),
    dividir: (a, b) => parseFloat(a) / parseFloat(b)
}